---
import { languages } from '../i18n/ui';
---
<ul class="flex flex-row gap-x-4 px-4">
  {Object.entries(languages).map(([lang, label]) => (
    <li>
      <a href={`/portfolio-dev/${lang}`}>{label}</a>
    </li>
  ))}
</ul>